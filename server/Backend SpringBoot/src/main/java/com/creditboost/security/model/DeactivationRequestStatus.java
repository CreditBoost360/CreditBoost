package com.creditboost.security.model;

/**
 * Enum representing the status of an admin deactivation request.
 */
public enum DeactivationRequestStatus {
    PENDING,
    APPROVED,
    DENIED,
    EXPIRED
}